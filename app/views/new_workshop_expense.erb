<section id="forms"  style="margin-left: 15%;">
    <div class="col-md-9"> 
    <br>
      <form class="form-horizontal well" action="<%=@callback_method %>" method="post" > 
        <fieldset>
          <legend>Create New Workshop Expense</legend>
          <br>
          <% if !@errors.empty? %>
             <legend style="color: red;  font-weight: bold; font-size: 14px;"
              <p>The Following Errors Were Encountered: </p>
              <% @errors.each do |e| %>
                <p><%=e%></p>
              <% end %>
            </legend>
          <% end %>
          <div clsss="row">
            <div class="col-md-5">
              <label class="control-label" for="textarea">Descripiton</label> 
              <input type="text" class="form-control" name="description" title="Enter Description" placeholder="Description" required >
            </div>  
            <div class="col-md-5">
              <label class="control-label" for="textarea">Amount</label> 
              <input type="text" class="form-control" name="amount" title="Amount" placeholder="Amount" required >
            </div>      
          </div>  
         <input type="hidden" name="workshop_id" value = "<%= @workshop_id%>">
        </fieldset> 
        <div clsss="row">
            <hr>
            <div class="form-actions">
              <button type="submit" class="btn btn-success" title="Select to Save Expsense Information">Save</button>
              <button type="reset" class="btn" onclick="history.go(-1);">Cancel</button>
            </div>
          </div>
      </form>
    </div> 
</section>
